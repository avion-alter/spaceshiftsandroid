<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dr.office">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.AddSpaceFourActivity"></activity>
        <activity android:name=".views.AddSpaceThreeActivity" />
        <activity android:name=".views.AddSpaceTwoActivity" />
        <activity android:name=".views.AddSpaceOneActivity" />
        <activity android:name=".views.ManageSpacesActivity" />
        <activity android:name=".views.FavouriteOfficeSpacesActivity" />
        <activity android:name=".views.SubscriptionsListActivity" />
        <activity android:name=".views.AccountActivity" />
        <activity android:name=".views.OfficeProfileActivity" />
        <activity
            android:name=".views.SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.MainActivity" />
        <activity
            android:name=".views.LoginSIgnUpActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.ForgotPasswordActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>